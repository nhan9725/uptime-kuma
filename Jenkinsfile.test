pipeline {
    agent  any
      //  kubernetes {
        //    yamlFile 'k8s/KubernetesPod.yaml'
      //  }
    
    stages {
        stage('Build') {
            stages {
                stage('Next.js Build') {
                    steps {
                        container('nextjs') {
                            sh 'npm install && npm run build'
                        }
                    }
                }
            }
        }
    }
}

